groups:
  - name: app-alerts
    rules:
      - alert: AppDown
        expr: up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Application is DOWN"
          description: "App is not reachable"
